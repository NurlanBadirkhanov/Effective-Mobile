<resources>

    <!-- Тема приложения -->
    <style name="Theme.Effective" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <!-- TextInput «пилюля» -->
    <style name="Widget.Effective.OutlinedPill" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundMode">outline</item>

        <item name="boxStrokeColor">@color/gray_700</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="hintTextColor">@color/gray_300</item>
        <item name="android:textColorHint">@color/gray_300</item>
    </style>

    <!-- Актив-индикатор bottom bar -->

     Формы

    <!-- ВРЕМЕННАЯ заглушка: если где-то осталась ссылка на @style/Widget -->
    <style name="Widget"/>
</resources>
